%LENGTH   Length of vector.
%   LENGTH(X) returns the length of vector X.  It is equivalent
%   to MAX(SIZE(X)) for non-empty arrays and 0 for empty ones.
%  
%   See also NUMEL.
%%   Documentation for length
%      doc length
%%   Other functions named length
%%      Bluetooth/length             icgroup/length
%      categorical/length           icsignal/length
%      cgrules/length               InputOutputModel/length
%      cgtradeoffkeytable/length    mbcstore/length
%      codistributed/length         opcroot/length
%      Composite/length             serial/length
%      coninputfactor/length        tabular/length
%      dataset/length               tall/length
%      datetime/length              tcpip/length
%      des_constraints/length       tscollection/length
%      designdev/length             udp/length
%      distributed/length           videoinput/length
%      fints/length                 visa/length
%      frac_lti/length              xregcontainer/length
%      frac_poly_exp/length         xregcovariance/length
%      gpib/length                  xregdataset/length
%      gpuArray/length              xregmonitorplotproperties/length
%      guidarray/length             xregmulti/length
%      i2c/length                   xregpointer/length
%      icdevice/length
%