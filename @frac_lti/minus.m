%-   Minus. 
%   X - Y subtracts matrix Y from X. X and Y must have compatible sizes. In
%   the simplest cases, they can be the same size or one can be a scalar.
%   Two inputs have compatible sizes if, for every dimension, the dimension
%   sizes of the inputs are either the same or one of them is 1.
%
%   C = MINUS(A,B) is called for the syntax 'A - B' when A or B is an
%   object.
%%   Documentation for minus
%      doc minus
%%   Other functions named minus
%%      calendarDuration/minus    gpuArray/minus
%      codistributed/minus       icsignal/minus
%      cvdata/minus              InputOutputModel/minus
%      datetime/minus            LagOp/minus
%      dlarray/minus             laurmat/minus
%      duration/minus            laurpoly/minus
%      fints/minus               mtree/minus
%      frac_lti/minus            sweepset/minus
%      frac_poly_exp/minus       sym/minus
%      frac_poly_imp/minus       tall/minus
%      frac_tf/minus             timeseries/minus
%